#!/bin/bash
# diff-zitadel-config.sh
# Stub script to compare local seed definitions against live ZITADEL config.

echo "Comparing local seed manifests against exported ZITADEL configuration..."
echo "This script is a stub. It will compare the output of export-zitadel-config.sh"
echo "with the YAML files in the seed/ directory."
